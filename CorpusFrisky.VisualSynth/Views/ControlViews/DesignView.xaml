<UserControl x:Class="CorpusFrisky.VisualSynth.Views.ControlViews.DesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Canvas x:Name="DesignCanvas"
                Background="Gray">
            <Canvas.ContextMenu>
                <ContextMenu x:Name="DesignContextMenu"
                             StaysOpen="True">
                    <MenuItem Header="Add">
                        <MenuItem x:Name="AddTriangleMenuItem" Header="Triangle"
                                  Command="{Binding AddTriangleCommand}" CommandParameter="{Binding ElementName=DesignCanvas}"/>
                        <MenuItem x:Name="AddRectangleMenuItem" Header="Rectangle"
                                  Command="{Binding AddRectangleCommand}" CommandParameter="{Binding ElementName=DesignCanvas}"/>
                    </MenuItem>
                </ContextMenu>
            </Canvas.ContextMenu>
            
        </Canvas>
            
    </Grid>
</UserControl>
